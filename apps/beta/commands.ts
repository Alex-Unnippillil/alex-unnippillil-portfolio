import type { Command } from '../../src/commands/types';

const commands: Command[] = [
  {
    id: 'beta',
    title: 'Beta command',
    keywords: ['beta'],
    run: () => {},
  },
];

export default commands;
